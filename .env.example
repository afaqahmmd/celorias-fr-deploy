# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Same value as CELORIAS_API_KEY in the backend .env — server-only, never expose to browser
CELORIAS_API_KEY=change-me-dashboard-key
